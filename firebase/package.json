{
  "name": "caregivers-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security-rules regression tests. CI gate for firebase/firestore.rules (Firestore emulator + @firebase/rules-unit-testing + mocha).",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project carerecipients \"mocha tests/firestore.rules.test.js --timeout 60000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^15.28.1",
    "mocha": "^11.8.0"
  }
}
